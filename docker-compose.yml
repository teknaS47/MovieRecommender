web:
  restart: always
  build: .
  command: python -u app.py
  ports:
    - "5000:5000"
  volumes:
    - .:/app
  links:
    - db

db:
  image: mongo
  ports:
    - 27017:27017
  # volumes:
  #   - ./the-movies-dataset:/app/data

db-seed:
  build: ./seed
  links: 
    - db